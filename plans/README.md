# Implementation Plans

Generated by the improve skill on 2026-08-05. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                                                          | Priority | Effort | Depends on | Status                                                                                    |
| ---- | ---------------------------------------------------------------------------------------------- | -------- | ------ | ---------- | ----------------------------------------------------------------------------------------- |
| 001  | Migrate `TodoTrackerSettingTab` to the Obsidian 1.13.0 declarative settings API                | P1       | L      | —          | DONE (full unit + integration suites green on Obsidian 1.13.4; see plan Execution record) |
| 002  | Replace native `window.confirm` with Obsidian `ConfirmationModal` for saved-search deletion    | P2       | S      | —          | DONE (full unit + integration suites green; 4 new tests pass; see plan Execution record)  |
| 003  | Replace Obsidian `Menu` class with custom `BaseDialog`-based state menu for consistent styling | P2       | M      | —          | TODO                                                                                      |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- None — 001 and 002 are independent.

## Findings considered and rejected

- `window.confirm` in `task-list-view.ts:1271`: the plugin's only native system dialog → addressed by plan 002.
- `window.open` docs URL in `search-options-dropdown.ts:180`: legitimate browser navigation, not a dialog; keep.
- `SavedSearchDialog` custom modal + native `<select>`s: migrating to Obsidian `Modal`/`Setting.addDropdown` would be nice-to-have but is deferred (independent M-sized change; deliberately out of plan 002's scope).
- `TaskContextMenu` custom menu: full migration to Obsidian `Menu` is L-effort/high-risk for a working UI (rich item rows, nested date-picker launch); deferred, not recommended now.
- `DatePicker` custom calendar: no public Obsidian date-picker API exists to migrate to; keep.
- Native `<select>` elements in view controls (`task-list-view.ts:571,659,715,776`): `Setting.addDropdown` only fits settings-row contexts; keep.
- State menu uses Obsidian `Menu` class with default styling instead of the custom `BaseDialog` pattern used by `TaskContextMenu` and `DatePicker`, causing inconsistent visual appearance across menus; addressed by plan 003.
